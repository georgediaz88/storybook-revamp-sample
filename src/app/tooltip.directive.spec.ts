import { TkTooltipDirective } from './tooltip.directive';

describe('TkTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new TkTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
